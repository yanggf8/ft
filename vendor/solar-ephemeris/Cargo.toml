# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.85"
name = "solar-ephemeris"
version = "0.2.0"
build = false
exclude = ["fuzz/"]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Zero-dependency VSOP2013 + ELP-MPP02 + TOP2013 ephemeris and topocentric sky engine, validated against JPL Horizons to arcsecond class"
readme = "README.md"
keywords = [
    "ephemeris",
    "astronomy",
    "vsop2013",
    "moon",
    "wasm",
]
categories = [
    "science",
    "aerospace",
    "wasm",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/Protonmatter/sol"

[lib]
name = "solar_ephemeris"
crate-type = [
    "cdylib",
    "rlib",
]
path = "src/lib.rs"

[[bin]]
name = "sky"
path = "src/bin/sky.rs"

[[example]]
name = "snapshot"
path = "examples/snapshot.rs"

[dependencies]
