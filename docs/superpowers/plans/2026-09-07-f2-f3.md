# F2 命盤象徵傾向 + F3 落差洞察 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 實作 F2(命盤→0–100 五維象徵向量,純規則)與 F3(實測 vs 象徵疊圖,落差 ≥20 才敘事,opt-in)。

**Architecture:** 零新表。規則表與聚合是 `ft-schema::symbolic` 純函數(native 可測);API 端 `resolve_chart` 型別化命盤解析器(抽自 charts route 的取圖/新鮮度邏輯)+ `GET /api/personality/overlay`;前端 `/personality` 結果頁 opt-in 疊圖(SVG 雷達 + 檔帶)。紅線:F5 生成路徑不得接觸 F2(型別隔離 + 選則輸出回歸釘死)。

**Tech Stack:** Rust workspace(worker-rs + Leptos CSR),Turso(Hrana over HTTP),既有 `db::Turso` helpers、engine service binding。

**Spec:** `docs/superpowers/specs/2026-09-07-f2-f3-design.md`(rev.2 — 執行者必讀,本計畫與它一起讀)。

## Global Constraints

- **禁止 `git commit`**:commit 由 owner(人類)執行;每個 Task 結束 = 回報審查點,不是 commit。
- 測試一律 native target:`timeout 300 cargo test -p <pkg> <filter>`;**禁止** `--target wasm32-unknown-unknown` 跑測試。cargo 指令一律 `timeout` 包。
- 每個 Task 結束跑 `timeout 300 cargo fmt --all`;不得新增 compiler warning(ft-api 現有 13 個、ft-web 現有 1 個為基準)。
- 不加新 dependency。Rust 2-space 縮排。
- 文案語言固定 zh-TW,verbatim 使用本計畫內文字;禁用詞表見 Task 4(含 log/變數名)。
- `crates/api/src/services/predictions.rs` 與 `crates/api/src/services/ai/` **不得** `use` 任何 `symbolic` 模組(Codex [1]/spec §0.4)。
- 維度順序恆為 E,A,C,S,O(index 0..4);wire 欄位名 `extraversion/agreeableness/conscientiousness/stability/openness`。
- 所有面向使用者的字串不得出現裸分(0–100 數字);一律檔帶。

## File Map

- Create: `crates/schema/src/symbolic.rs` — 規則表、`SymbolicVector`、`Band`、`symbolic_vector()`、檔帶/落差/敘事純函數、DTO(全 native 測試)。
- Modify: `crates/schema/src/lib.rs` — `pub mod symbolic;`
- Create: `crates/api/src/services/chart_resolver.rs` — 型別化命盤解析(決策純函數 native 測試;IO 膠水不 native 測)。
- Modify: `crates/api/src/services/mod.rs` — `pub mod chart_resolver;`
- Modify: `crates/api/src/routes/personality.rs` — `GET /api/personality/overlay`。
- Modify: `crates/schema/src/predict.rs` — 釘死 F5 選則輸出的回歸測試(僅測試)。
- Modify: `crates/web/src/api.rs` — overlay client + 型別。
- Modify: `crates/web/src/pages/personality.rs` — opt-in 疊圖 UI。

---

### Task 1: 規則表 + 型別 + 檔帶(`symbolic.rs` 骨架)

**Files:**
- Create: `crates/schema/src/symbolic.rs`
- Modify: `crates/schema/src/lib.rs`(加 `pub mod symbolic;`,緊鄰現有 `pub mod predict;`)

**Interfaces(後續任務依賴):**
- `pub const SYMBOLIC_RULES_VERSION: &str = "symbolic-1"`
- `pub const DIM_CODES: [&str; 5] = ["extraversion", "agreeableness", "conscientiousness", "stability", "openness"]`
- `pub const DIM_LABELS: [&str; 5] = ["外向", "友善", "嚴謹", "情緒穩定", "智性開放"]`
- `pub enum Band { Low, MidLow, Mid, MidHigh, High }` + `band_of(f64) -> Band` + `Band::code()/Band::label()/band_label(&str) -> &'static str`
- `pub enum PriorSource { Ziwei, Western }`、`pub struct SymbolicVector { ruleset_version, source, dims: [f64;5] }`

- [ ] **Step 1: 註冊模組 + 寫失敗測試** — **先**在 `crates/schema/src/lib.rs` 加 `pub mod symbolic;`(緊鄰 `pub mod predict;`;未註冊的檔案不會被編譯,RED 會變成零測試的假綠燈 — Codex [19]),再建 `crates/schema/src/symbolic.rs`,只放測試模組與必要的型別宣告殼(型別本體下一 step 才寫;以「函數不存在」的編譯錯誤為 RED,屬預期):

```rust
//! F2 命盤象徵傾向 — 純規則對照表與聚合(spec 2026-09-07-f2-f3-design.md §1)。
//! 紅線:本模組不得被 predictions.rs / services::ai import(spec §0.4)。

#[cfg(test)]
mod tests {
    use super::*;
    use crate::ZiWeiStarV3;

    #[test]
    fn band_boundaries_are_exact() {
        assert_eq!(band_of(34.9), Band::Low);
        assert_eq!(band_of(35.0), Band::MidLow);
        assert_eq!(band_of(44.9), Band::MidLow);
        assert_eq!(band_of(45.0), Band::Mid);
        assert_eq!(band_of(55.0), Band::Mid);
        assert_eq!(band_of(55.1), Band::MidHigh);
        assert_eq!(band_of(65.0), Band::MidHigh);
        assert_eq!(band_of(65.1), Band::High);
    }

    #[test]
    fn band_codes_and_labels_are_paired() {
        for (b, code, label) in [
            (Band::Low, "low", "偏低"),
            (Band::MidLow, "mid_low", "略低"),
            (Band::Mid, "mid", "中等"),
            (Band::MidHigh, "mid_high", "略高"),
            (Band::High, "high", "偏高"),
        ] {
            assert_eq!(b.code(), code);
            assert_eq!(b.label(), label);
            assert_eq!(band_label(code), label);
        }
    }

    #[test]
    fn rule_table_values_are_on_grid_and_cover_all_stars() {
        const MAIN: [&str; 14] = ["紫微", "天機", "太陽", "武曲", "天同", "廉貞", "天府",
            "太陰", "貪狼", "巨門", "天相", "天梁", "七殺", "破軍"];
        for (star, _dim, delta, _basis) in ZIWEI_RULES {
            assert!(MAIN.contains(star), "unknown star {star}");
            assert!(matches!(*delta, -20 | -10 | 10 | 20), "off-grid delta {delta}");
        }
        for star in MAIN {
            assert!(ZIWEI_RULES.iter().any(|(s, _, _, _)| s == &star), "{star} missing");
        }
        // 依據標註(spec §1.3 雙軸):以下 7 條是設計裁量,其餘古典(Codex [14])
        for (s, d) in [("紫微", 0usize), ("紫微", 4), ("武曲", 0), ("廉貞", 3),
                       ("天府", 0), ("太陰", 4), ("巨門", 0), ("七殺", 3)] {
            assert!(ZIWEI_RULES.iter().any(|(st, dim, _, b)| st == s && *dim == d && *b == Basis::Designer),
                "{s}/{d} 應標 designer");
        }
        assert!(ZIWEI_RULES.iter().any(|(st, dim, _, b)| st == "太陽" && *dim == 0 && *b == Basis::Classical));
    }
}
```

- [ ] **Step 2: 跑測試確認 RED**:`timeout 300 cargo test -p ft-schema symbolic::` — 預期編譯錯誤(`band_of`/`ZIWEI_RULES` 不存在)。
- [ ] **Step 3: 最小實作** — 同檔加:

```rust
pub const SYMBOLIC_RULES_VERSION: &str = "symbolic-1";
pub const DIM_CODES: [&str; 5] = ["extraversion", "agreeableness", "conscientiousness", "stability", "openness"];
pub const DIM_LABELS: [&str; 5] = ["外向", "友善", "嚴謹", "情緒穩定", "智性開放"];

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Band { Low, MidLow, Mid, MidHigh, High }

pub fn band_of(score: f64) -> Band {
    if score < 35.0 { Band::Low }
    else if score < 45.0 { Band::MidLow }
    else if score <= 55.0 { Band::Mid }
    else if score <= 65.0 { Band::MidHigh }
    else { Band::High }
}

impl Band {
    pub fn code(self) -> &'static str {
        match self { Band::Low => "low", Band::MidLow => "mid_low", Band::Mid => "mid",
            Band::MidHigh => "mid_high", Band::High => "high" }
    }
    pub fn label(self) -> &'static str {
        match self { Band::Low => "偏低", Band::MidLow => "略低", Band::Mid => "中等",
            Band::MidHigh => "略高", Band::High => "偏高" }
    }
}

pub fn band_label(code: &str) -> &'static str {
    match code { "low" => "偏低", "mid_low" => "略低", "mid" => "中等",
        "mid_high" => "略高", _ => "偏高" }
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum PriorSource { Ziwei, Western }

/// 規則「依據」標註(spec §1.3 雙軸:依據 × 映射;映射一律 designer,不入表)。
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Basis { Classical, Designer }

#[derive(Clone, Debug)]
pub struct SymbolicVector {
    pub ruleset_version: &'static str,
    pub source: PriorSource,
    pub dims: [f64; 5],
    /// 每維貢獻規則的依據彙總:"classical" | "designer" | "mixed"
    pub dim_basis: [&'static str; 5],
}

/// (主星名, 維度 index 0..4 = E,A,C,S,O, 調整量, 依據)。缺席的維 = 無貢獻、
/// 不進分母(spec §1.3 聚合公約;Codex [6])。
pub const ZIWEI_RULES: &[(&str, usize, i32, Basis)] = &[
    ("紫微", 2, 20, Basis::Classical), ("紫微", 0, 10, Basis::Designer), ("紫微", 4, -10, Basis::Designer),
    ("天機", 4, 20, Basis::Classical), ("天機", 3, -10, Basis::Classical), ("天機", 2, -10, Basis::Classical),
    ("太陽", 0, 20, Basis::Classical), ("太陽", 1, 10, Basis::Classical),
    ("武曲", 2, 20, Basis::Classical), ("武曲", 1, -10, Basis::Classical), ("武曲", 0, -10, Basis::Designer),
    ("天同", 3, 20, Basis::Classical), ("天同", 2, -20, Basis::Classical), ("天同", 1, 10, Basis::Classical),
    ("廉貞", 2, 10, Basis::Classical), ("廉貞", 4, 10, Basis::Classical), ("廉貞", 3, -10, Basis::Designer),
    ("天府", 2, 20, Basis::Classical), ("天府", 3, 10, Basis::Classical), ("天府", 0, -10, Basis::Designer),
    ("太陰", 0, -20, Basis::Classical), ("太陰", 4, 10, Basis::Designer), ("太陰", 3, -10, Basis::Classical),
    ("貪狼", 0, 20, Basis::Classical), ("貪狼", 4, 10, Basis::Classical), ("貪狼", 2, -10, Basis::Classical),
    ("巨門", 4, 20, Basis::Classical), ("巨門", 1, -10, Basis::Classical), ("巨門", 3, -10, Basis::Classical), ("巨門", 0, -10, Basis::Designer),
    ("天相", 1, 20, Basis::Classical), ("天相", 2, 10, Basis::Classical),
    ("天梁", 3, 10, Basis::Classical), ("天梁", 1, 10, Basis::Classical), ("天梁", 2, 10, Basis::Classical),
    ("七殺", 0, 10, Basis::Classical), ("七殺", 1, -10, Basis::Classical), ("七殺", 3, -10, Basis::Designer),
    ("破軍", 4, 20, Basis::Classical), ("破軍", 2, -20, Basis::Classical), ("破軍", 0, 10, Basis::Classical),
];
```

(`pub mod symbolic;` 註冊已在 Step 1 完成。)
- [ ] **Step 4: 跑測試確認 GREEN**:`timeout 300 cargo test -p ft-schema symbolic::` — 全 PASS。
- [ ] **Step 5:** `cargo fmt --all` 後回報審查點(不 commit)。

### Task 2: 聚合 — 紫微命宮主星(含借宮 clamp)

**Files:**
- Modify: `crates/schema/src/symbolic.rs`

**Interfaces:**
- `pub fn ziwei_life_stars(palaces: &[ZiWeiPalaceV3], life_palace_index: u8) -> Vec<(&str, bool)>` — `(主星名, borrowed)`
- `fn adjustments_for(stars: &[(&str, bool)]) -> [Option<f64>; 5]`(私有;借星逐條 clamp ±10 **在平均前**)
- 後續任務依賴:`pub fn symbolic_vector(ziwei: Option<(&[ZiWeiPalaceV3], u8)>, western: Option<(&str, &str, &str)>) -> Option<SymbolicVector>`(本 task 先實作 ziwei 分支 + None;western 分支 Task 3 補)

- [ ] **Step 1: 失敗測試** — tests 模組加:

```rust
    fn star(name: &str) -> ZiWeiStarV3 {
        ZiWeiStarV3 { name: name.to_string(), star_type: "major".into(), brightness: None, sihua: None }
    }
    fn palace(index: u8, stars: Vec<ZiWeiStarV3>) -> ZiWeiPalaceV3 {
        ZiWeiPalaceV3 { index, name: String::new(), branch: String::new(), stem: String::new(),
            stars, is_life_palace: None, is_body_palace: None }
    }

    #[test]
    fn life_stars_taken_from_life_palace_only() {
        let palaces = vec![palace(0, vec![star("紫微"), star("天府")]), palace(1, vec![star("貪狼")])];
        let got = ziwei_life_stars(&palaces, 0);
        assert_eq!(got, vec![("紫微", false), ("天府", false)]);
    }

    #[test]
    fn empty_life_palace_borrows_opposite() {
        let palaces = vec![palace(0, vec![]), palace(6, vec![star("太陽")])];
        let got = ziwei_life_stars(&palaces, 0);
        assert_eq!(got, vec![("太陽", true)]);
    }

    #[test]
    fn omitted_dims_do_not_enter_denominator() {
        // 紫微+天府:O 僅紫微 -10 命中 -> 40(非 45)。Codex [6] fixture。
        let v = symbolic_vector(Some((&[palace(0, vec![star("紫微"), star("天府")])], 0)), None).unwrap();
        assert_eq!(v.source, PriorSource::Ziwei);
        assert_eq!(v.dims[4], 40.0);
        // C:紫微+20、天府+20 平均 -> 70;E:紫微+10、天府-10 平均 -> 50
        assert_eq!(v.dims[2], 70.0);
        assert_eq!(v.dims[0], 50.0);
        // A 無命中 -> 50;S 僅天府 +10 -> 60(Codex [P1] 修正:原 fixture 算錯)
        assert_eq!(v.dims[1], 50.0);
        assert_eq!(v.dims[3], 60.0);
    }

    #[test]
    fn borrowed_pojun_all_dims_clamped() {
        // 借破軍:O+20→+10、C-20→-10、E+10→+10(Codex [17] fixture)
        let v = symbolic_vector(Some((&[palace(0, vec![]), palace(6, vec![star("破軍")])], 0)), None).unwrap();
        assert_eq!(v.dims[4], 60.0);
        assert_eq!(v.dims[2], 40.0);
        assert_eq!(v.dims[0], 60.0);
    }

    #[test]
    fn borrowed_star_clamped_per_rule_before_averaging() {
        // 借破軍(O+20 被 clamp 成 +10)-> O = 60,不是 70。
        let v = symbolic_vector(Some((&[palace(0, vec![]), palace(6, vec![star("破軍")])], 0)), None).unwrap();
        assert_eq!(v.dims[4], 60.0);
    }

    #[test]
    fn no_charts_yields_none() {
        assert!(symbolic_vector(None, None).is_none());
    }
```

- [ ] **Step 2: RED** — 同 Step 1 之 Task 1 指令;預期 `ziwei_life_stars`/`symbolic_vector` 不存在。
- [ ] **Step 3: 實作:**

```rust
use crate::ZiWeiPalaceV3;

const MAIN_STARS: [&str; 14] = ["紫微", "天機", "太陽", "武曲", "天同", "廉貞", "天府",
    "太陰", "貪狼", "巨門", "天相", "天梁", "七殺", "破軍"];

/// 命宮主星(含借對宮,borrowed = true)。四化/亮度/身宮不計(spec §1.3)。
pub fn ziwei_life_stars(palaces: &[ZiWeiPalaceV3], life_palace_index: u8) -> Vec<(&str, bool)> {
    let at = |idx: u8| palaces.iter().find(|p| p.index == idx);
    let life = at(life_palace_index);
    let direct: Vec<(&str, bool)> = life
        .map(|p| p.stars.iter().filter(|s| MAIN_STARS.contains(&s.name.as_str()))
            .map(|s| (s.name.as_str(), false)).collect())
        .unwrap_or_default();
    if !direct.is_empty() {
        return direct;
    }
    let opp = (life_palace_index + 6) % 12;
    at(opp)
        .map(|p| p.stars.iter().filter(|s| MAIN_STARS.contains(&s.name.as_str()))
            .map(|s| (s.name.as_str(), true)).collect())
        .unwrap_or_default()
}

/// 每維命中調整的平均;無命中 = None。借星逐條 clamp [−10,10] 在平均前。
fn adjustments_for(stars: &[(&str, bool)]) -> [Option<f64>; 5] {
    let mut sums = [0f64; 5];
    let mut counts = [0usize; 5];
    for (star, borrowed) in stars {
        for (_s, dim, delta, _basis) in ZIWEI_RULES.iter().filter(|(s, _, _, _)| s == star) {
            let mut d = *delta as f64;
            if *borrowed { d = d.clamp(-10.0, 10.0); }
            sums[*dim] += d;
            counts[*dim] += 1;
        }
    }
    std::array::from_fn(|i| (counts[i] > 0).then(|| sums[i] / counts[i] as f64))
}

pub fn symbolic_vector(
    ziwei: Option<(&[ZiWeiPalaceV3], u8)>,
    western: Option<(&str, &str, &str)>,
) -> Option<SymbolicVector> {
    if let Some((palaces, idx)) = ziwei {
        let stars = ziwei_life_stars(palaces, idx);
        let adj = adjustments_for(&stars);
        let dims = std::array::from_fn(|i| adj[i].map_or(50.0, |a| (50.0 + a).clamp(0.0, 100.0)));
        return Some(SymbolicVector {
            ruleset_version: SYMBOLIC_RULES_VERSION,
            source: PriorSource::Ziwei,
            dims,
            dim_basis: basis_summary(&stars),
        });
    }
    if let Some((sun, moon, asc)) = western {
        let adj = western_adjustments(sun, moon, asc);
        let dims = std::array::from_fn(|i| adj[i].map_or(50.0, |a| (50.0 + a).clamp(0.0, 100.0)));
        return Some(SymbolicVector {
            ruleset_version: SYMBOLIC_RULES_VERSION,
            source: PriorSource::Western,
            dims,
            dim_basis: ["classical"; 5],
        });
    }
    None
}

/// 每維貢獻規則的依據彙總(Codex [14]:UI 的來源行由此驅動,不寫死)。
fn basis_summary(stars: &[(&str, bool)]) -> [&'static str; 5] {
    let mut has_c = [false; 5];
    let mut has_d = [false; 5];
    for (star, _borrowed) in stars {
        for (_s, dim, _delta, basis) in ZIWEI_RULES.iter().filter(|(s, _, _, _)| s == star) {
            match basis {
                Basis::Classical => has_c[*dim] = true,
                Basis::Designer => has_d[*dim] = true,
            }
        }
    }
    std::array::from_fn(|i| match (has_c[i], has_d[i]) {
        (true, true) => "mixed",
        (false, true) => "designer",
        _ => "classical",
    })
}

fn western_adjustments(_sun: &str, _moon: &str, _asc: &str) -> [Option<f64>; 5] {
    std::array::from_fn(|_| None) // Task 3 實作
}
```

- [ ] **Step 4: GREEN** — 全 PASS(注意 `omitted_dims...` 的 E=50、S=60 斷言)。
- [ ] **Step 5:** `cargo fmt --all`;回報審查點。

### Task 3: 西洋元素分支 + 全盤制優先序

**Files:**
- Modify: `crates/schema/src/symbolic.rs`

**Interfaces:**
- `fn element(sign: &str) -> Option<usize>`(私有;回傳維度 index)— 中英文星座名都吃; `fn western_adjustments(sun, moon, asc) -> [Option<f64>; 5]`(太陽 ±20、月亮/上升 ±10;缺席維不進分母,同紫微公約)
- `symbolic_vector` 兩盤都給時**必取紫微**(spec §1.1 全盤制)

- [ ] **Step 1: 失敗測試** — tests 加:

```rust
    #[test]
    fn western_element_rules_with_weights() {
        // sun=牡羊(火,E+20) moon=Cancer(水,S-10) asc=Leo(火,E+10)
        let v = symbolic_vector(None, Some(("牡羊座", "Cancer", "Leo"))).unwrap();
        assert_eq!(v.source, PriorSource::Western);
        assert_eq!(v.dims[0], 65.0); // E:(20+10)/2=15 -> 65
        assert_eq!(v.dims[3], 40.0); // S:-10 -> 40
        assert_eq!(v.dims[1], 50.0); // 無命中 -> 50
    }

    #[test]
    fn ziwei_wins_when_both_present() {
        let p = [palace(0, vec![star("紫微")])];
        let v = symbolic_vector(Some((&p, 0)), Some(("Aries", "Aries", "Aries"))).unwrap();
        assert_eq!(v.source, PriorSource::Ziwei);
        assert_eq!(v.dims[2], 70.0); // 紫微 C+20,不是西洋土象的值
    }

    #[test]
    fn english_sign_names_match_too() {
        let v = symbolic_vector(None, Some(("Capricorn", "Virgo", "Taurus"))).unwrap();
        assert!((v.dims[2] - (50.0 + 40.0 / 3.0)).abs() < 1e-9); // C 三條命中平均 13.33
    }

    #[test]
    fn all_12_signs_in_all_three_bodies_are_recognized() {
        const SIGNS: [&str; 12] = ["Aries", "Taurus", "Gemini", "Cancer", "Leo", "Virgo",
            "Libra", "Scorpio", "Sagittarius", "Capricorn", "Aquarius", "Pisces"];
        for s in SIGNS {
            for (sun, moon, asc) in [(s, "Aries", "Aries"), ("Aries", s, "Aries"), ("Aries", "Aries", s)] {
                let v = symbolic_vector(None, Some((sun, moon, asc))).unwrap();
                // 元素必命中 E/C/S/O 之一,故必有一維偏離 50(Codex [17] 覆蓋)
                assert!(v.dims.iter().any(|&d| (d - 50.0).abs() > f64::EPSILON), "{s} unmapped");
            }
        }
    }

    #[test]
    fn dim_basis_mixed_and_designer() {
        // 紫微單星:E/O designer、C classical(Codex [14] fixture)
        let v = symbolic_vector(Some((&[palace(0, vec![star("紫微")])], 0)), None).unwrap();
        assert_eq!(v.dim_basis[0], "designer");
        assert_eq!(v.dim_basis[4], "designer");
        assert_eq!(v.dim_basis[2], "classical");
        // 紫微+太陽同宮:E 一 designer 一 classical -> mixed
        let v = symbolic_vector(Some((&[palace(0, vec![star("紫微"), star("太陽")])], 0)), None).unwrap();
        assert_eq!(v.dim_basis[0], "mixed");
    }

- [ ] **Step 2: RED** — `timeout 300 cargo test -p ft-schema symbolic::`,預期 western 測試失敗(現在恆回 50/None 分支)。
- [ ] **Step 3: 實作** — 取代 Task 2 的 `western_adjustments` stub:

```rust
fn element(sign: &str) -> Option<usize> {
    let s = sign.trim().trim_end_matches('座');
    const FIRE: [&str; 6] = ["牡羊", "獅子", "射手", "Aries", "Leo", "Sagittarius"];
    const EARTH: [&str; 6] = ["金牛", "處女", "摩羯", "Taurus", "Virgo", "Capricorn"];
    const AIR: [&str; 6] = ["雙子", "天秤", "水瓶", "Gemini", "Libra", "Aquarius"];
    const WATER: [&str; 6] = ["巨蟹", "天蠍", "雙魚", "Cancer", "Scorpio", "Pisces"];
    let hit = |names: &[&str; 6]| names.iter().any(|n| s.eq_ignore_ascii_case(n));
    if hit(&FIRE) { Some(0) } else if hit(&EARTH) { Some(2) }
    else if hit(&AIR) { Some(4) } else if hit(&WATER) { Some(3) } else { None }
}

fn western_adjustments(sun: &str, moon: &str, asc: &str) -> [Option<f64>; 5] {
    let mut sums = [0f64; 5];
    let mut counts = [0usize; 5];
    for (sign, delta) in [(sun, 20.0), (moon, 10.0), (asc, 10.0)] {
        if let Some(dim) = element(sign) {
            // 水象是 S 的負向調整(spec §1.3:水 -> S−;Codex [P2])
            sums[dim] += if dim == 3 { -delta } else { delta };
            counts[dim] += 1;
        }
    }
    std::array::from_fn(|i| (counts[i] > 0).then(|| sums[i] / counts[i] as f64))
}
```

- [ ] **Step 4: GREEN** — 全 PASS(含 Task 2 既有測試不迴歸)。
- [ ] **Step 5:** `cargo fmt --all`;回報審查點。

### Task 4: F3 決策 — gap / narrativeOk / 敘事文字 / DTO

**Files:**
- Modify: `crates/schema/src/symbolic.rs`
- Modify: `crates/schema/Cargo.toml` — 僅當 `[dev-dependencies]` 無 `serde_json` 時加(工作區其他 crate 已用,版本對齊工作區)。

**Interfaces(後續任務依賴):**
- `pub fn narrative_ok(prior: Option<f64>, measured: f64) -> bool`
- `pub struct DimOutcome { … }`(serde camelCase;wire 見 spec §3)
- `pub fn dim_outcomes(measured: [f64; 5], prior: Option<&SymbolicVector>) -> Vec<DimOutcome>`
- `pub const BANNED_WORDS: &[&str]`(spec §2.4 禁用詞表)
- `pub fn narrative_text(dim_index: usize, prior_band: Band, measured_band: Band) -> String`

- [ ] **Step 1: 失敗測試** — tests 加:

```rust
    #[test]
    fn narrative_gate_is_absolute_both_directions_and_exempt_low_band() {
        assert!(narrative_ok(Some(60.0), 40.0)); // |40-60|=20 過門檻
        assert!(narrative_ok(Some(40.0), 60.0)); // 反方向同樣過
        assert!(!narrative_ok(Some(50.0), 39.9)); // 10.1 < 20
        assert!(!narrative_ok(Some(69.9), 50.0)); // 19.9 < 20(邊界下;Codex [17])
        assert!(narrative_ok(Some(70.0), 50.0)); // 20.0 恰過(反方向)
        assert!(!narrative_ok(Some(50.0), 30.0)); // gap 20 但實測偏低檔 -> 豁免(Codex [5])
        assert!(!narrative_ok(None, 10.0)); // F2 缺席 -> 不敘事
    }

    #[test]
    fn dim_outcomes_wire_shape_and_texts() {
        let prior = SymbolicVector { ruleset_version: SYMBOLIC_RULES_VERSION, source: PriorSource::Ziwei, dim_basis: ["classical"; 5], dims: [60.0, 50.0, 50.0, 50.0, 50.0] };
        let out = dim_outcomes([40.0, 50.0, 50.0, 50.0, 50.0], Some(&prior));
        assert_eq!(out.len(), 5);
        assert_eq!(out[0].dim, "extraversion");
        assert_eq!(out[0].gap, Some(20.0));
        assert!(out[0].narrative_ok);
        assert!(out[0].text.as_deref().unwrap_or("").contains("命盤象徵落在"));
        assert!(out[0].text.as_deref().unwrap_or("").contains("略高")); // 檔帶措詞,非二分高低
        assert!(!out[1].narrative_ok);
        assert!(out[1].text.is_none());
        assert!(out[1].gap.unwrap().abs() < f64::EPSILON);
        // F2 缺席:prior/gap/priorBand 全 null、全不敘事
        let none = dim_outcomes([40.0, 50.0, 50.0, 50.0, 50.0], None);
        assert!(none.iter().all(|d| d.text.is_none() && d.prior.is_none()));
    }

    #[test]
    fn dim_outcome_serializes_camelcase_with_band_codes() {
        let prior = SymbolicVector { ruleset_version: SYMBOLIC_RULES_VERSION, source: PriorSource::Ziwei, dim_basis: ["classical"; 5], dims: [40.0; 5] };
        let out = dim_outcomes([70.0, 50.0, 50.0, 50.0, 50.0], Some(&prior));
        let json: serde_json::Value = serde_json::to_value(&out).unwrap();
        assert_eq!(json[0]["dim"], "extraversion");
        assert_eq!(json[0]["priorBand"], "mid_low"); // prior 40
        assert_eq!(json[0]["measuredBand"], "high"); // measured 70
        assert_eq!(json[0]["gap"], 30.0); // |70-40|(原 20 為 fixture 算術誤植)
        assert_eq!(json[0]["narrativeOk"], true);
        assert_eq!(json[0]["basis"], "classical");
        assert_eq!(json[1]["measuredBand"], "mid"); // measured 50
        assert_eq!(json[1]["narrativeOk"], false);
        assert!(json[1]["text"].is_null());
    }

    #[test]
    fn generated_text_never_contains_banned_words() {
        // 極端組合:每維都產生敘事,掃禁用詞(spec §2.4)
        let prior = SymbolicVector { ruleset_version: SYMBOLIC_RULES_VERSION, source: PriorSource::Ziwei, dim_basis: ["classical"; 5], dims: [30.0; 5] };
        let out = dim_outcomes([70.0, 70.0, 70.0, 70.0, 70.0], Some(&prior));
        for d in &out {
            if let Some(t) = &d.text {
                for w in BANNED_WORDS {
                    assert!(!t.contains(w), "{w} appeared in: {t}");
                }
            }
        }
    }
```

- [ ] **Step 2: RED** — 預期 `narrative_ok`/`dim_outcomes`/`BANNED_WORDS` 不存在。
- [ ] **Step 3: 實作:**

```rust
use serde::Serialize;

pub const BANNED_WORDS: &[&str] = &["神經質", "憂鬱", "焦慮症", "診斷", "病態",
    "缺陷", "低能", "人格障礙", "違背天性", "壓抑本性"];

pub fn narrative_ok(prior: Option<f64>, measured: f64) -> bool {
    match prior {
        None => false,
        Some(p) => (measured - p).abs() >= 20.0 && band_of(measured) != Band::Low,
    }
}

pub fn narrative_text(dim_index: usize, prior_band: Band, measured_band: Band) -> String {
    format!(
        "在{}上,命盤象徵落在{},你的實測落在{}。象徵是傳統對照,實測是你作答的結果;這段落差只是描述,不含評價,也不暗示該怎麼改。",
        DIM_LABELS[dim_index], prior_band.label(), measured_band.label()
    )
}

#[derive(Serialize, Deserialize, Debug, Clone)]
#[serde(rename_all = "camelCase")]
pub struct DimOutcome {
    pub dim: String,
    pub measured: f64,
    pub prior: Option<f64>,
    /// 檔帶 enum code:low / mid_low / mid / mid_high / high
    pub prior_band: Option<String>,
    pub measured_band: String,
    pub gap: Option<f64>,
    pub narrative_ok: bool,
    /// 該維象徵規則依據彙總:classical | designer | mixed(prior 缺席 = None;
    /// Codex [14]:UI 來源行由此驅動)
    pub basis: Option<String>,
    pub text: Option<String>,
}

/// overlay 回應外殼 — API 與 web 共用此型別(Codex [22]:不重複宣告)。
/// 欄位用 String 而非 &'static str:serde 零拷貝限制下,含 &'static str 的
/// 結構體無法從 owned String 反序列化(Deserialize derive 編譯不過)。
#[derive(Serialize, Deserialize, Debug, Clone)]
#[serde(rename_all = "camelCase")]
pub struct OverlayResponse {
    pub ruleset_version: String,
    pub prior_source: Option<String>,
    pub birth_known: bool,
    pub dims: Vec<DimOutcome>,
}

pub fn dim_outcomes(measured: [f64; 5], prior: Option<&SymbolicVector>) -> Vec<DimOutcome> {
    DIM_CODES.iter().enumerate().map(|(i, &code)| {
        let m_band = band_of(measured[i]);
        let p = prior.map(|v| v.dims[i]);
        let ok = narrative_ok(p, measured[i]);
        DimOutcome {
            dim: code.to_string(),
            measured: measured[i],
            prior: p,
            prior_band: prior.map(|v| band_of(v.dims[i]).code().to_string()),
            measured_band: m_band.code().to_string(),
            gap: p.map(|v| (measured[i] - v).abs()),
            narrative_ok: ok,
            basis: prior.map(|v| v.dim_basis[i].to_string()),
            text: ok.then(|| {
                let pb = prior.map(|v| band_of(v.dims[i])).unwrap_or(Band::Mid);
                narrative_text(i, pb, m_band)
            }),
        }
    }).collect()
}
```

(若 `prior_band` 那行 borrow 寫不攏,等價改法:`let prior_band = prior.and_then(|v| Some(band_of(v.dims[i]).code()));` — 語意一致即可,以測試為準。)
- [ ] **Step 4: GREEN** — 全 PASS。
- [ ] **Step 5:** `cargo fmt --all`;回報審查點。

### Task 5: overlay 資格判定矩陣(API 層,純函數)

**Files:**
- Modify: `crates/api/src/routes/personality.rs`

**Interfaces:**
- `pub(crate) enum OverlayEligibility { Allowed, F3Disabled, MeasurementPending, NoMeasurement }`
- `pub(crate) fn overlay_eligibility(latest_status: Option<&str>) -> OverlayEligibility`
- 後續:Task 7 的 route 以此決定 409 三態或繼續。

- [ ] **Step 1: 失敗測試** — `personality.rs` 檔尾加測試模組:

```rust
#[cfg(test)]
mod overlay_eligibility_tests {
    use super::*;

    #[test]
    fn matrix_matches_spec() {
        assert!(matches!(overlay_eligibility(Some("complete")), OverlayEligibility::Allowed));
        assert!(matches!(overlay_eligibility(Some("skipped_prior_only")), OverlayEligibility::F3Disabled));
        assert!(matches!(overlay_eligibility(Some("careless_suspected")), OverlayEligibility::MeasurementPending));
        assert!(matches!(overlay_eligibility(None), OverlayEligibility::NoMeasurement));
        // 未知狀態 fail-closed(Codex [2])
        assert!(matches!(overlay_eligibility(Some("??")), OverlayEligibility::NoMeasurement));
    }
}
```

- [ ] **Step 2: RED** — `timeout 300 cargo test -p ft-api overlay_eligibility`。
- [ ] **Step 3: 實作** — `personality.rs` 頂層:

```rust
/// spec §2.3 資格判定矩陣:最新一列 profile(`created_at DESC, rowid DESC`)的
/// 狀態治理;歷史結果的重新查看不改變判定(Codex [2])。未知狀態 fail-closed。
pub(crate) enum OverlayEligibility { Allowed, F3Disabled, MeasurementPending, NoMeasurement }

pub(crate) fn overlay_eligibility(latest_status: Option<&str>) -> OverlayEligibility {
    match latest_status {
        Some("complete") => OverlayEligibility::Allowed,
        Some("skipped_prior_only") => OverlayEligibility::F3Disabled,
        Some("careless_suspected") => OverlayEligibility::MeasurementPending,
        _ => OverlayEligibility::NoMeasurement,
    }
}
```

- [ ] **Step 4: GREEN** — PASS。
- [ ] **Step 5:** `cargo fmt --all`;回報審查點。

### Task 6: 命盤解析器(chart_resolver)

**Files:**
- Create: `crates/api/src/services/chart_resolver.rs`
- Modify: `crates/api/src/services/mod.rs`(加 `pub mod chart_resolver;`,比照其他 mod 行)
- **明確不改**:`crates/api/src/routes/charts.rs`、`crates/api/src/routes/common.rs`(Codex [P12]:charts route 行為不變;`embed_meta` 留在原地,resolver 內聯等價的新鮮度判斷)

**Interfaces:**
- `pub(crate) fn chart_usable(stored_birth_hash: &str, birth_hash: &str, stored_engine_version: &str, engine_version: &str, stored_schema_version: u32, schema_version: u32) -> bool`
- `pub(crate) fn cached_payload_current(raw: Option<&str>, stored_birth_hash: Option<&str>, birth_hash: &str, engine_version_key: &str, expected_engine_version: &str, expected_schema_version: u32) -> bool` — 在 `serde_json::Value` 層檢查:缺 raw/壞 JSON/列上無雜湊/雜湊不合/meta 缺 key/版本不合 → false(Codex [P10]:`WesternChartV3` 結構體無 meta 欄位,新鮮度必須在 Value 層判,typed 反序列化在最後一步)
- `pub(crate) fn resolve_plan(has_birth: bool, has_gender: bool) -> (bool, bool)` — `(try_ziwei, try_western)`;這是「嘗試資格」,不是「兩者都算」(Codex [P11])
- `pub(crate) struct ResolvedCharts { pub ziwei: Option<ZiWeiChartV3>, pub western: Option<WesternChartV3>, pub birth_known: bool }`
- `pub(crate) async fn resolve_chart(ctx: &RouteContext<()>, user_id: &str) -> Result<ResolvedCharts, worker::Response>`

**v1 簡化(rev.3 spec §1.4 已同步):** resolver **只讀+即時算,不寫快取** — 命中新鮮快取就讀,否則經 engine binding 現算但不落 `interpretations`。重算清 `ai_interpretation` 的副作用與並發寫入問題因此在 overlay 路徑不存在;重複計算成本可接受。

- [ ] **Step 0: 對照實際簽名與儲存形狀**(膠水層以實際為準;決策純函數與測試不受影響):

```bash
rg -n 'pub async fn' crates/api/src/services/engine.rs
rg -n 'engineVersion|chartSchemaVersion' crates/api/src/routes/common.rs | head -10   # embed_meta 寫進 JSON 的 key 名
rg -n -A 10 'CREATE TABLE IF NOT EXISTS interpretations' scripts/schema.sql           # 快取列欄位名
rg -n 'fn error_code' crates/api/src/error.rs                                          # Task 7 用
```

- [ ] **Step 1: 失敗測試** — 新檔先放測試:

```rust
//! 型別化命盤解析(spec §1.4):新鮮快取優先,gender 缺失跳紫微不發明預設,
//! 絕不回退過期盤。v1 只讀不寫快取。

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn usable_requires_hash_and_versions_aligned() {
        assert!(chart_usable("h1", "h1", "4.0.0", "4.0.0", 3, 3));
        assert!(!chart_usable("h1", "h2", "4.0.0", "4.0.0", 3, 3)); // birth 變了
        assert!(!chart_usable("h1", "h1", "3.0.0", "4.0.0", 3, 3)); // engine 過期
        assert!(!chart_usable("h1", "h1", "4.0.0", "4.0.0", 2, 3)); // schema 過期
    }

    #[test]
    fn plan_skips_ziwei_without_gender() {
        assert_eq!(resolve_plan(true, true), (true, true));
        assert_eq!(resolve_plan(true, false), (false, true)); // 不發明 gender
        assert_eq!(resolve_plan(false, true), (false, false)); // 無生辰 -> 全跳
        assert_eq!(resolve_plan(false, false), (false, false));
    }

    const FRESH_ZIWEI: &str =
        r#"{"meta":{"engineVersionZiwei":"4.0.0","chartSchemaVersion":3}}"#;

    #[test]
    fn cached_payload_fresh_stale_malformed_absent() {
        assert!(cached_payload_current(Some(FRESH_ZIWEI), Some("h1"), "h1", "engineVersionZiwei", "4.0.0", 3));
        assert!(!cached_payload_current(Some(FRESH_ZIWEI), Some("h1"), "h2", "engineVersionZiwei", "4.0.0", 3)); // 雜湊不合
        assert!(!cached_payload_current(Some(FRESH_ZIWEI), None, "h1", "engineVersionZiwei", "4.0.0", 3)); // 列上無雜湊 = miss
        assert!(!cached_payload_current(Some(r#"{"meta":{}}"#), Some("h1"), "h1", "engineVersionZiwei", "4.0.0", 3)); // 缺 key
        assert!(!cached_payload_current(Some(r#"{"meta":{"engineVersionZiwei":"3.0.0","chartSchemaVersion":3}}"#), Some("h1"), "h1", "engineVersionZiwei", "4.0.0", 3)); // 版本舊
        assert!(!cached_payload_current(Some("{not json"), Some("h1"), "h1", "engineVersionZiwei", "4.0.0", 3)); // 壞 JSON
        assert!(!cached_payload_current(None, Some("h1"), "h1", "engineVersionZiwei", "4.0.0", 3)); // 缺 raw
    }
}
```

(meta key 名以 Step 0 實查為準,fixture 跟著改。)
- [ ] **Step 2: RED** — `timeout 300 cargo test -p ft-api chart_resolver`。
- [ ] **Step 3: 實作決策函數 + 膠水:**

```rust
use ft_schema::{WesternChartV3, ZiWeiChartV3};
use crate::services::{db, engine, engine_version};
use serde_json::Value;
use worker::{RouteContext, Response};

pub(crate) fn chart_usable(stored_birth_hash: &str, birth_hash: &str,
    stored_engine_version: &str, engine_version: &str,
    stored_schema_version: u32, schema_version: u32) -> bool {
    stored_birth_hash == birth_hash
        && stored_engine_version == engine_version
        && stored_schema_version == schema_version
}

pub(crate) fn cached_payload_current(raw: Option<&str>, stored_birth_hash: Option<&str>,
    birth_hash: &str, engine_version_key: &str, expected_engine_version: &str,
    expected_schema_version: u32) -> bool {
    let Some(raw) = raw else { return false };
    let Ok(v) = serde_json::from_str::<Value>(raw) else { return false };
    let Some(stored) = stored_birth_hash else { return false };
    if stored != birth_hash { return false; }
    let ev = v.get("meta").and_then(|m| m.get(engine_version_key)).and_then(Value::as_str);
    let sv = v.get("meta").and_then(|m| m.get("chartSchemaVersion")).and_then(Value::as_u64);
    ev == Some(expected_engine_version) && sv == Some(expected_schema_version as u64)
}

pub(crate) fn resolve_plan(has_birth: bool, has_gender: bool) -> (bool, bool) {
    (has_birth && has_gender, has_birth)
}

pub(crate) struct ResolvedCharts {
    pub ziwei: Option<ZiWeiChartV3>,
    pub western: Option<WesternChartV3>,
    /// 使用者是否已填生辰 — UI 以此區分「未填生辰」與「命盤暫時無法計算」
    /// 兩態(spec §4 不得共用文案;Codex [3])。
    pub birth_known: bool,
}

/// 生辰定義:birth_year/month/day 皆非 NULL;hour NULL 沿用 charts.rs 現行 fallback。
/// gender NULL → 跳紫微,不發明預設(Codex [3]:engine client 會靜默 default male)。
///
/// 膠水(Codex [P10][P11][P12]):只讀不寫;紫微可用即早返回,西洋僅作 fallback;
/// 絕不回退過期盤;engine/parse 失敗 = 該盤不可用;DB 失敗 = 5xx。
/// 實作時對照 Step 0 的實際簽名調整呼叫處;本 task 不修改 charts.rs/common.rs。
pub(crate) async fn resolve_chart(ctx: &RouteContext<()>, user_id: &str)
    -> Result<ResolvedCharts, Response>
{
    let _ = (ctx, user_id);
    // 1. db::Turso::from_env;讀 users 一列:birth_year/month/day/hour、gender、
    //    timezone、latitude、longitude、birth_data_hash(欄位名以 schema.sql 為準)。
    // 2. has_birth = 三個生辰欄皆非 NULL;has_gender = gender IS NOT NULL。
    //    let (try_ziwei, try_western) = resolve_plan(has_birth, has_gender);
    //    birth_known = has_birth。
    // 3. try_ziwei 時:SELECT chart_data, birth_data_hash FROM interpretations
    //    WHERE user_id = ?1 AND divination_type = 'ziwei'
    //    → cached_payload_current(...) 為真 → serde_json::from_str::<ZiWeiChartV3>
    //       採用並**早返回**(不碰西洋;Codex [P11]);
    //    否則經 engine 現算(呼叫簽名以 Step 0 實查為準)→ from_value 採用;
    //    現算/解析失敗 → ziwei = None,不回退過期盤。
    // 4. ziwei 為 None 且 try_western:對 'western' 重複步驟 3(新鮮度檢查同樣用
    //    列上 birth_data_hash;西洋 JSON 若無 meta,一律視為 miss → 現算,註解記錄)。
    // 5. DB 錯誤 → Err(error::error("db unavailable", 500)) 同格式。
    Ok(ResolvedCharts { ziwei: None, western: None, birth_known: false })
}
```

- [ ] **Step 4: GREEN** — 決策函數測試 PASS(膠水編譯過即可;真實行為走 Task 11 runtime 驗收)。
- [ ] **Step 5:** `timeout 300 cargo fmt --all`;回報審查點。

### Task 7: `GET /api/personality/overlay` 路由

**Files:**
- Modify: `crates/api/src/routes/personality.rs`(在 `register()` 鏈上加一條)

**Interfaces:**
- `GET /api/personality/overlay`(Bearer session 必需)
- 200:`{ "rulesetVersion": "symbolic-1", "priorSource": "ziwei"|"western"|null, "birthKnown": bool, "dims": [DimOutcome;5] }`
- 409(帶結構化 code,web 端 `is_code` 分流 — Codex [P8]):`NO_MEASUREMENT` / `F3_DISABLED` / `MEASUREMENT_PENDING`;401 未認證(沿用 `auth_user`)

- [ ] **Step 0: 對照現有形狀:**

```bash
rg -n 'fn error_code' crates/api/src/error.rs                       # 帶 code 的錯誤 helper
rg -n 'measurement_status|ocean_measured' crates/api/src/routes/personality.rs | head  # 既有最新列 SELECT 慣例
rg -n 'struct OceanScores' ~/.cargo/../../$(pwd)/crates/schema/src -A 10  # 或 rg -n 'struct OceanScores' crates/schema/src -A 10
```

- [ ] **Step 1: 實作路由 + 量測解析**(端點無 native 測試 — wasm 限制;決策邏輯已在 Task 2/4/5/6 native 覆蓋,`parse_ocean` 有 native 測試):

```rust
        .get_async("/api/personality/overlay", |req, ctx| async move {
            let user_id = match auth_user(&req, &ctx).await {
                Ok(u) => u,
                Err(r) => return Ok(r),
            };
            let db = match db::Turso::from_env(&ctx.env) {
                Ok(d) => d,
                Err(_) => return Ok(error::error("db unavailable", 500)),
            };
            let uid = db::text(&user_id);
            // 資格 + 量測**同一列**(Codex [P9]:兩次查詢之間可能插入新測驗,
            // 會違反「最新列狀態治理」契約)。型別沿用人格路由既有 row struct 慣例。
            let latest: Option<LatestProfileRow> = db::first(
                &db,
                "SELECT measurement_status, ocean_measured FROM personality_profiles \
                 WHERE user_id = ?1 ORDER BY created_at DESC, rowid DESC LIMIT 1",
                &[&uid],
            ).await;
            let row = match latest {
                Ok(Some(r)) => r,
                Ok(None) => return Ok(error::error_code("no measurement", "NO_MEASUREMENT", 409)),
                Err(_) => return Ok(error::error("db error", 500)),
            };
            match overlay_eligibility(row.measurement_status.as_deref()) {
                OverlayEligibility::F3Disabled => {
                    return Ok(error::error_code("f3 disabled", "F3_DISABLED", 409))
                }
                OverlayEligibility::MeasurementPending => {
                    return Ok(error::error_code("measurement pending", "MEASUREMENT_PENDING", 409))
                }
                OverlayEligibility::NoMeasurement => {
                    return Ok(error::error_code("no measurement", "NO_MEASUREMENT", 409))
                }
                OverlayEligibility::Allowed => {}
            }
            let Some(measured) = parse_ocean(row.ocean_measured.as_deref()) else {
                // complete 列缺/壞量測 = 資料異常 — 500 但不外洩細節
                return Ok(error::error("db error", 500));
            };
            let charts = match chart_resolver::resolve_chart(&ctx, &user_id).await {
                Ok(c) => c,
                Err(r) => return Ok(r),
            };
            let prior = symbolic::symbolic_vector(
                charts.ziwei.as_ref().map(|c| (c.palaces.as_slice(), c.life_palace_index)),
                charts.western.as_ref().map(|c| (
                    symbolic::western_sign_name(&c.sun_sign),
                    symbolic::western_sign_name(&c.moon_sign),
                    c.ascendant.sign.as_str())),
            );
            let prior_source = prior.as_ref().map(|v| match v.source {
                symbolic::PriorSource::Ziwei => "ziwei", symbolic::PriorSource::Western => "western",
            });
            let dims = symbolic::dim_outcomes(measured, prior.as_ref());
            Ok(ok_json(&serde_json::json!({
                "rulesetVersion": symbolic::SYMBOLIC_RULES_VERSION,
                "priorSource": prior_source,
                "birthKnown": charts.birth_known,
                "dims": dims,
            }), 200))
        })
```

**注意**:route closure 回 `Result<Response, worker::Error>` — 所有 `Response` 形錯誤
一律 `match` + `return Ok(...)`,**不得用 `?`**(Codex [P5];worker 0.8.5 router 釘死此簽名)。
配套定義(放在 personality.rs 頂層,緊鄰 `overlay_eligibility`):

```rust
#[derive(serde::Deserialize)]
struct LatestProfileRow {
    measurement_status: Option<String>,
    ocean_measured: Option<String>,
}

/// ocean_measured JSON -> [f64;5],順序 E,A,C,S,O(spec §3)。
/// 欄位對照 `ft_schema::api::OceanScores` 宣告順序單點映射(Step 0 核對欄名);
/// 缺欄/壞 JSON/非有限值 -> None。
fn parse_ocean(raw: Option<&str>) -> Option<[f64; 5]> {
    let v: ft_schema::api::OceanScores = serde_json::from_str(raw?).ok()?;
    let out = [v.extraversion, v.agreeableness, v.conscientiousness, v.stability, v.openness];
    out.iter().all(|x| x.is_finite()).then_some(out)
}

#[cfg(test)]
mod overlay_parse_tests {
    use super::*;

    #[test]
    fn parse_ocean_maps_five_dims_in_order() {
        let raw = r#"{"extraversion":61.7,"agreeableness":50.0,"conscientiousness":40.0,"stability":30.0,"openness":62.0}"#;
        assert_eq!(parse_ocean(Some(raw)), Some([61.7, 50.0, 40.0, 30.0, 62.0]));
        assert!(parse_ocean(Some("not json")).is_none());
        assert!(parse_ocean(None).is_none());
    }
}
```

(若 `OceanScores` 欄位名不同,以實查為準改 `parse_ocean` 的映射行 — 映射只此一處。)
- [ ] **Step 2:** `timeout 300 cargo test -p ft-api overlay` — PASS;`timeout 300 cargo check -p ft-api` — 無新 warning。
- [ ] **Step 3:** `timeout 300 cargo fmt --all`;回報審查點。

### Task 8: F5 紅線 — 選則輸出回歸釘死

**Files:**
- Modify: `crates/schema/src/predict.rs`(僅測試模組)

**Interfaces:**
- golden 回歸測試:`select_for_domain(Domain, [f64;5], [u8;5]) -> Option<Selected>`(簽名以 `crates/schema/src/predict.rs:64` 實查為準)與 `filter_negative_half` 對固定輸入的完整輸出被逐欄位釘死:`anchor.id`、`anchor.domain`、`anchor.tendency`、`anchor.forecast`、`anchor.experiment`、`anchor_ids`、`trigger`、`coverage`、`valence`(Codex [P13]:不得用 contains/len 弱斷言)。

- [ ] **Step 1: 產生 golden 值** — 寫暫時測試,固定輸入**一律**:
  scores = `[70.0, 40.0, 55.0, 30.0, 62.0]`、ranges = `[0; 5]`,分別對
  `Domain::Work` 與 `Domain::Money` 呼叫 `select_for_domain`;把兩個 `Option<Selected>`
  與 `filter_negative_half(vec![work, money])` 的完整內容用 `--nocapture` 印出
  (`timeout 300 cargo test -p ft-schema predict:: -- --nocapture`),抄進 Step 2。
  再做一組全低分 `scores = [20.0; 5]` 的負向 fixture 同樣印出(`filter_negative_half`
  的 D2-A 例外路徑要被釘到)。
- [ ] **Step 2: 寫釘死測試**(形狀如下;值以 Step 1 實際輸出為準):

```rust
    #[test]
    fn f5_selection_outputs_are_pinned_against_chart_side_effects() {
        // 紅線(spec §0.4):命盤/F2 相關改動若改變了 F5 選則輸出,這裡必炸。
        // golden 值首次撰寫時由 --nocapture 實跑抄錄;更新必須在 code review
        // 中說明「為何 F5 輸出會變」。
        const SCORES: [f64; 5] = [70.0, 40.0, 55.0, 30.0, 62.0];
        let work = select_for_domain(Domain::Work, SCORES, [0; 5]).expect("work 應有選則");
        // 逐欄位斷言(golden 抄錄):work.anchor.id / .domain / .tendency /
        // .forecast / .experiment、work.anchor_ids、work.trigger、work.coverage、
        // work.valence — 全部 == golden 值。
        let money = select_for_domain(Domain::Money, SCORES, [0; 5]).expect("money 應有選則");
        // 同上逐欄位斷言。
        let filtered = filter_negative_half(vec![work, money]);
        // 斷言 filtered 的長度與逐元素逐欄位 == golden 值。
    }

    #[test]
    fn f5_all_negative_fixture_pinned() {
        // 全負分 fixture:filter_negative_half 的保留較佳 1 條(D2-A)被釘死
        const LOW: [f64; 5] = [20.0; 5];
        // select 兩領域 -> filter -> 斷言保留那一條的完整欄位 == golden 值。
    }
```

- [ ] **Step 3: GREEN + 驗證紅線** — `timeout 300 cargo test -p ft-schema predict::` PASS;
  另跑 `rg -n 'symbolic' crates/api/src/services/predictions.rs crates/api/src/services/ai/` —
  預期**零輸出**(有輸出 = 違反全域約束,回到對應 Task 修)。
- [ ] **Step 4:** `timeout 300 cargo fmt --all`;回報審查點。

### Task 9: web — overlay client

**Files:**
- Modify: `crates/web/src/api.rs`(放在 `check_invite` 附近)

**Interfaces:**
- `pub async fn fetch_overlay() -> Result<ft_schema::symbolic::OverlayResponse, ApiErr>` — 型別直接用 ft-schema 共用 DTO,**不重複宣告**(Codex [P22];本專案慣例:web 直接用 `ft-schema` 型別)

- [ ] **Step 0:** `rg -n 'fn get_json' crates/web/src/api.rs` — 確認帶 auth 的 JSON GET helper 名稱與簽名(既有 client 經它附 Bearer header;Codex [P7]:自己 `Request::get` 不帶 header,連登入用戶都會 401)。
- [ ] **Step 1: 實作:**

```rust
/// `GET /api/personality/overlay` — F3 疊圖(spec 2026-09-07-f2-f3)。409 三態
/// (NO_MEASUREMENT / F3_DISABLED / MEASUREMENT_PENDING)由呼叫端以 ApiErr::is_code 分流。
pub async fn fetch_overlay() -> Result<ft_schema::symbolic::OverlayResponse, ApiErr> {
    get_json("/api/personality/overlay", true).await
}
```

(helper 名稱/簽名以 Step 0 實查為準;語意以「帶 auth 的 JSON GET」為準。)
- [ ] **Step 2:** `timeout 540 cargo check -p ft-web --target wasm32-unknown-unknown` — 過、無新 warning。
- [ ] **Step 3:** `timeout 300 cargo fmt --all`;回報審查點。

### Task 10: web — `/personality` 疊圖 UI(opt-in)

**Files:**
- Modify: `crates/web/src/pages/personality.rs`(結果區塊內加一段)

**Interfaces:**
- 消費 Task 9 的 `fetch_overlay`;型別 `ft_schema::symbolic::{OverlayResponse, DimOutcome, DIM_CODES, DIM_LABELS, band_label}`。
- 異型別分支一律 `.into_any()`(本檔既有先例;Codex [P6]:Leptos 0.8 `into_view` 保留具體型別)。

- [ ] **Step 1: 實作**。在結果區塊(view 尾端)加入以下區段;文案 verbatim:

```rust
    // ── F3 疊圖(spec 2026-09-07-f2-f3 §2.3):一律 opt-in,不自動顯示 ──
    let overlay = RwSignal::new(Option::<ft_schema::symbolic::OverlayResponse>::None);
    let overlay_error = RwSignal::new(String::new());
    let overlay_stage = RwSignal::new(0u8); // 0=未展開 1=說明頁 2=疊圖
    let open_overlay = move |_| {
        // 進入前固定說明(spec §2.3;此文案為禁用詞白名單,經 owner 核可)
        overlay_stage.set(1);
    };
    let load_overlay = move |_| {
        spawn_local(async move {
            match api::fetch_overlay().await {
                Ok(v) => {
                    overlay.set(Some(v));
                    overlay_stage.set(2);
                }
                Err(e) => {
                    overlay_error.set(if e.is_code("F3_DISABLED") {
                        "你目前選擇僅使用命盤象徵、未完成人格測驗,對照功能未開啟。".to_string()
                    } else if e.is_code("MEASUREMENT_PENDING") {
                        "測驗結果尚待確認,完成有效測驗後即可對照。".to_string()
                    } else if e.is_code("NO_MEASUREMENT") {
                        "完成人格測驗後即可對照。".to_string()
                    } else {
                        e.to_string()
                    });
                    overlay_stage.set(2);
                }
            }
        });
    };
```

view 區段(接在現有結果之後)。**規則**(Codex [P15][P16]):200 時**實測檔帶列一律
渲染**;「未填生辰/命盤暫時無法計算」訊息只是加在上方,不得擠掉實測列;雷達僅在
prior 在場時畫;軸上不標裸分、標籤用檔帶;S 軸標明「高=穩」:

```rust
                        <Show when=move || overlay_stage.get() == 0 fallback=|| ()>
                            <div style="margin-top:2rem;text-align:center">
                                <button class="btn-link" on:click=open_overlay>
                                    "查看命盤象徵對照"
                                </button>
                            </div>
                        </Show>
                        <Show when=move || overlay_stage.get() == 1 fallback=|| ()>
                            <div class="card" style="margin-top:1.5rem">
                                <p style="line-height:1.9">
                                    "「命盤象徵傾向」來自出生盤的傳統星性對照,「實測人格」來自你剛完成的人格測驗。兩條線來源不同,不一致很常見;不一致不代表你有缺陷,也不代表你需要改變。只有兩者差距夠大時,我們才會描述那個落差。"
                                </p>
                                <button class="btn-primary" style="width:100%" on:click=load_overlay>
                                    "我知道了,看對照"
                                </button>
                            </div>
                        </Show>
                        <Show when=move || overlay_stage.get() == 2 fallback=|| ()>
                            {move || {
                                let err = overlay_error.get();
                                if !err.is_empty() { return view! { <p class="error">{err}</p> }.into_any(); }
                                let Some(v) = overlay.get() else { return view! { <div/> }.into_any(); };
                                let missing_note = if !v.birth_known {
                                    Some("填寫生辰後即可和命盤對照。".to_string())
                                } else if v.prior_source.is_none() {
                                    Some("命盤暫時無法計算,請稍後再試。".to_string())
                                } else { None };
                                let note_view = missing_note.map(|n| view! {
                                    <p class="center-note">{n}</p>
                                });
                                // 雷達:五軸 E,A,C,S,O;實線=實測、虛線=命盤象徵;
                                // 軸上不標裸分;0 分就在圓心,不外推(Codex [P16])
                                let pts = |vals: Vec<f64>| -> String {
                                    vals.iter().enumerate().map(|(i, s)| {
                                        let ang = -90.0f64 + 72.0 * i as f64;
                                        let r = 80.0 * (s / 100.0).clamp(0.0, 1.0);
                                        let (x, y) = (100.0 + r * ang.to_radians().cos(), 100.0 + r * ang.to_radians().sin());
                                        format!("{x:.1},{y:.1}")
                                    }).collect::<Vec<_>>().join(" ")
                                };
                                let measured_pts = pts(v.dims.iter().map(|d| d.measured).collect());
                                let prior_pts = pts(v.dims.iter().map(|d| d.prior.unwrap_or(50.0)).collect());
                                let axis_labels = ["E 外向", "A 友善", "C 嚴謹", "S 情緒穩定(高=穩)", "O 智性開放"];
                                let dim_label = |code: &str| -> &'static str {
                                    let i = ft_schema::symbolic::DIM_CODES.iter()
                                        .position(|&c| c == code).unwrap_or(0);
                                    ft_schema::symbolic::DIM_LABELS[i]
                                };
                                let band_label = |code: &str| ft_schema::symbolic::band_label(code);
                                let basis_label = |b: Option<&str>| match b {
                                    Some("classical") => "依據:古典星性",
                                    Some("designer") => "依據:設計裁量",
                                    Some("mixed") => "依據:古典星性 + 設計裁量",
                                    _ => "—",
                                };
                                let radar = if v.prior_source.is_some() {
                                    view! {
                                        <svg viewBox="0 0 200 200" style="width:min(320px,80%);display:block;margin:0 auto">
                                            <polygon points={pts(vec![100.0;5])} fill="none" stroke="#e5e7eb"/>
                                            <polygon points={measured_pts.clone()} fill="rgba(59,130,246,0.12)" stroke="#3b82f6" stroke-width="2"/>
                                            <polygon points={prior_pts.clone()} fill="none" stroke="#9aa3b2" stroke-width="2" stroke-dasharray="5,4"/>
                                            {axis_labels.iter().enumerate().map(|(i, label)| {
                                                let ang = -90.0f64 + 72.0 * i as f64;
                                                let (x, y) = (100.0 + 92.0 * ang.to_radians().cos(), 100.0 + 92.0 * ang.to_radians().sin());
                                                view! {
                                                    <text x={x} y={y} text-anchor="middle" style="font-size:7px;fill:#6b7280">{*label}</text>
                                                }
                                            }).collect::<Vec<_>>()}
                                        </svg>
                                        <p style="font-size:0.75rem;color:#9aa3b2;text-align:center">
                                            "實線 = 你的實測;虛線 = 命盤象徵傾向"
                                        </p>
                                    }.into_any()
                                } else { view! { <div/> }.into_any() };
                                view! {
                                    <div class="card" style="margin-top:1.5rem">
                                        <h2>"命盤象徵對照"</h2>
                                        {note_view}
                                        {radar}
                                        {v.dims.iter().map(|d| view! {
                                            <div style="padding:0.4rem 0;border-top:1px solid #f3f4f6">
                                                <div style="display:flex;justify-content:space-between">
                                                    <span>{dim_label(&d.dim)}</span>
                                                    <span style="color:#6b7280">
                                                        {format!("命盤象徵 {} · 你的實測 {}",
                                                            d.prior_band.as_deref().map(band_label).unwrap_or("—"),
                                                            band_label(&d.measured_band))}
                                                    </span>
                                                </div>
                                                <details>
                                                    <summary style="font-size:0.75rem;color:#9aa3b2">"來源"</summary>
                                                    <span style="font-size:0.75rem;color:#9aa3b2">
                                                        {format!("{};與人格測驗分數無關。", basis_label(d.basis.as_deref()))}
                                                    </span>
                                                </details>
                                            </div>
                                        }).collect::<Vec<_>>()}
                                        {v.dims.iter().filter(|d| d.narrative_ok).map(|d| view! {
                                            <p style="line-height:1.9;margin-top:0.8rem">{d.text.clone().unwrap_or_default()}</p>
                                        }).collect::<Vec<_>>()}
                                    </div>
                                }.into_any()
                            }
                        </Show>
```

(巢狀閉包若與本檔既有 Leptos 慣用法衝突,以檔內寫法調整**語法**;文案、兩態訊息
不加擠掉實測列、`into_any`、共用標籤函數等**語意不得改**。)
- [ ] **Step 2:** `timeout 540 cargo check -p ft-web --target wasm32-unknown-unknown` — 過、無新 warning。
- [ ] **Step 3:** `timeout 300 cargo fmt --all`;回報審查點。

### Task 11: 全鏈驗證 + 交接

- [ ] **Step 1: native 全測** — `timeout 540 cargo test -p ft-schema -p ft-api` 全 PASS。
- [ ] **Step 2: wasm 檢查** — `timeout 540 cargo check -p ft-web --target wasm32-unknown-unknown` **和** `timeout 540 cargo check -p ft-api --target wasm32-unknown-unknown` 都過(Codex [P23]:新 Worker 整合也要 target 檢查)。
- [ ] **Step 3: 紅線靜態檢查(範圍限定 — Codex [P18])**:
  - `rg -n 'symbolic' crates/api/src/services/predictions.rs crates/api/src/services/ai/` → 零輸出。
  - 禁用詞只掃**新檔案**:`rg -n '神經質|neurotic' crates/api/src/services/chart_resolver.rs crates/schema/src/symbolic.rs` → 零輸出。`BANNED_WORDS` 字典本身、測試 fixture、spec 核可的說明文案、既有檔案(如 `crates/domain/big5/src/scoring.rs:2`)為豁免 — **不得**為了過關去改字典、測試或既有檔。
- [ ] **Step 4:** `timeout 300 cargo fmt --all -- --check` 乾淨;warning 數不超基準(ft-api 13 / ft-web 1)。
- [ ] **Step 5: 驗收對照矩陣(spec § → task;Codex [P17])**:

| spec 節 | 落點 |
|---|---|
| §0 紅線/機制 | T8 回歸釘死 + T11 Step 3 import 掃描 + 全域約束 |
| §1.1 全盤制/基準 50 | T2/T3 測試 |
| §1.2 檔帶 | T1 邊界 fixture + T4 wire |
| §1.3 規則表/借星/聚合/依據 | T1 不變式 + T2 借破軍三維 fixture + T3 basis fixture |
| §1.4 解析器 | T6(fresh/stale/malformed/absent 四態) |
| §2.1/2.2 門檻/豁免 | T4(20 與 19.9、兩方向、豁免壓過大 gap) |
| §2.3 資格矩陣/動線 | T5 測試 + T7 parse_ocean 測試 + T10 文案 |
| §2.4 模板/禁用詞 | T4 禁用詞掃描測試 |
| §3 API 契約 | T4 序列化測試 + T7 |
| §4 UI 兩態/來源行 | T10 |
| §5 native 測試 | T1-T5、T7、T8 |
| §5 runtime 驗收 | Step 6(owner 執行,待辦) |
| §6/§7 已知風險與不做 | 無實作項 |

- [ ] **Step 6: 回報** — 彙整所有審查點與驗證輸出,**不 commit、不部署**。以下 runtime 驗收為 **owner 部署後執行**,狀態=待辦,不得標記完成:409 三態實測、過期快取重算、gender 缺失跳紫微、engine 失敗降級、並發重複計算容忍、四態命盤(缺席/新鮮/過期/失敗)下 F5 生成不變(spec §0.4)、前端 opt-in 動線與兩態文案、**§2.4 模板與說明文案真人終審**。

## 交接

本計畫依 owner 指定的流程執行:計畫先送 Codex 對抗審查 → Claude corroborate →
以 superpowers:subagent-driven-development 派工(Grok 寫、Claude 逐行審);
每 Task 的「回報審查點」是強制停損點,不通過不進下一 Task。

