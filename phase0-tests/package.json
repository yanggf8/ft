{
  "name": "phase0-tests",
  "version": "1.0.0",
  "description": "FortuneT V2 Phase 0 Risk Assessment Tests",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:local": "wrangler dev --local",
    "db:create": "wrangler d1 create fortunet-test-db",
    "db:init": "wrangler d1 execute fortunet-test-db --local --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.51.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.5.0"
  }
}
